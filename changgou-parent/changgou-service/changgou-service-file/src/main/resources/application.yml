server:
  port: 8300
spring:
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  application:
    name: file
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka
feign:
  hystrix:
    enabled: true